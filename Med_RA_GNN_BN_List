/* a list of RA related medicine, with GenericName & BrandNames */

description=upcase(description);

if index(description,'ABATACEPT') or index(description,'ORENCIA') then GNN='ABATACEPT';
else if index(description,'ADALIMUMAB') or index(description,'HUMIRA') then GNN='ADALIMUMAB';
else if index(description,'ANAKINRA') or index(description,'KINERET') then GNN='ANAKINRA';
else if index(description,'APREMILAST') or index(description,'OTEZLA') then GNN='APREMILAST';
else if index(description,'AZATHIOPRINE') or index(description,'AZASAN') then GNN='AZATHIOPRINE';
else if index(description,'AZATHIOPRINE') or index(description,'AZATHIOPRINE') then GNN='AZATHIOPRINE';
else if index(description,'AZATHIOPRINE') or index(description,'IMURAN') then GNN='AZATHIOPRINE';
else if index(description,'BELIMUMAB') or index(description,'BENLYSTA') then GNN='BELIMUMAB';
else if index(description,'CANAKINUMAB') or index(description,'ILARIS') then GNN='CANAKINUMAB';
else if index(description,'CERTOLIZUMAB') or index(description,'CIMZIA') then GNN='CERTOLIZUMAB';
else if index(description,'CYCLOPHOSPHAMIDE') or index(description,'CYCLOPHOSPHAMIDE') then GNN='CYCLOPHOSPHAMIDE';
else if index(description,'CYCLOPHOSPHAMIDE') or index(description,'CYTOXAN') then GNN='CYCLOPHOSPHAMIDE';
else if index(description,'CYCLOPHOSPHAMIDE') or index(description,'NEOSAR') then GNN='CYCLOPHOSPHAMIDE';
else if index(description,'CYCLOSPORINE') or index(description,'CYCLOSPORINE') then GNN='CYCLOSPORINE';
else if index(description,'CYCLOSPORINE') or index(description,'GENGRAF') then GNN='CYCLOSPORINE';
else if index(description,'CYCLOSPORINE') or index(description,'NEORAL') then GNN='CYCLOSPORINE';
else if index(description,'CYCLOSPORINE') or index(description,'SANDIMMUNE') then GNN='CYCLOSPORINE';
else if index(description,'CYCLOSPORINE') or index(description,'SANGCYA') then GNN='CYCLOSPORINE';
else if index(description,'EFALIZUMAB') or index(description,'RAPTIVA') then GNN='EFALIZUMAB';
else if index(description,'ETANERCEPT') or index(description,'ENBREL') then GNN='ETANERCEPT';
else if index(description,'GOLIMUMAB') or index(description,'SIMPONI') then GNN='GOLIMUMAB';
else if index(description,'HYDROXYCHLOROQUINE') or index(description,'HYDROXYCHLOROQUINE') then GNN='HYDROXYCHLOROQUINE';
else if index(description,'HYDROXYCHLOROQUINE') or index(description,'PLAQUENIL') then GNN='HYDROXYCHLOROQUINE';
else if index(description,'HYDROXYCHLOROQUINE') or index(description,'QUINEPROX') then GNN='HYDROXYCHLOROQUINE';
else if index(description,'INFLIXIMAB') or index(description,'REMICADE') then GNN='INFLIXIMAB';
else if index(description,'INFLIXIMAB-DYYB') or index(description,'INFLECTRA') then GNN='INFLIXIMAB-DYYB';
else if index(description,'IXEKIZUMAB')  then GNN='IXEKIZUMAB';
else if index(description,'LEFLUNOMIDE') or index(description,'ARAVA') then GNN='LEFLUNOMIDE';
else if index(description,'LEFLUNOMIDE') or index(description,'LEFLUNOMIDE') then GNN='LEFLUNOMIDE';
else if index(description,'MERCAPTOPURINE') or index(description,'MERCAPTOPURINE') then GNN='MERCAPTOPURINE';
else if index(description,'MERCAPTOPURINE') or index(description,'PURINETHOL') then GNN='MERCAPTOPURINE';
else if index(description,'MERCAPTOPURINE') or index(description,'PURIXAN') then GNN='MERCAPTOPURINE';
else if index(description,'MESALAMINE') or index(description,'APRISO') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'ASACOL') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'ASACOL HD') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'CANASA') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'DELZICOL') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'FIV-ASA') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'LIALDA') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'MESALAMINE') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'PENTASA') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'ROWASA') then GNN='MESALAMINE';
else if index(description,'MESALAMINE') or index(description,'SFROWASA') then GNN='MESALAMINE';
else if index(description,'METHOTREXATE') or index(description,'ABITREXATE') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'FOLEX PFS') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'FOLEX-PFS') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'METHOTREXATE') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'MEXATE') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'MEXATE-AQ') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'OTREXUP') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'RASUVO') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'RHEUMATREX') then GNN='METHOTREXATE';
else if index(description,'METHOTREXATE') or index(description,'TREXALL') then GNN='METHOTREXATE';
else if index(description,'NATALIZUMAB') or index(description,'TYSABRI') then GNN='NATALIZUMAB';
else if index(description,'RILONACEPT') or index(description,'ARCALYST') then GNN='RILONACEPT';
else if index(description,'RITUXIMAB') or index(description,'RITUXAN') then GNN='RITUXIMAB';
else if index(description,'SECUKINUMAB') or index(description,'COSENTYX') then GNN='SECUKINUMAB';
else if index(description,'SULFASALAZINE') or index(description,'AZULFIDINE') then GNN='SULFASALAZINE';
else if index(description,'SULFASALAZINE') or index(description,'S.A.S.-500') then GNN='SULFASALAZINE';
else if index(description,'SULFASALAZINE') or index(description,'SULFA DYNE') then GNN='SULFASALAZINE';
else if index(description,'SULFASALAZINE') or index(description,'SULFASALAZINE') then GNN='SULFASALAZINE';
else if index(description,'SULFASALAZINE') or index(description,'SULFAZINE') then GNN='SULFASALAZINE';
else if index(description,'TACROLIMUS') or index(description,'ASTAGRAF XL') then GNN='TACROLIMUS';
else if index(description,'TACROLIMUS') or index(description,'ENVARSUS XR') then GNN='TACROLIMUS';
else if index(description,'TACROLIMUS') or index(description,'HECORIA') then GNN='TACROLIMUS';
else if index(description,'TACROLIMUS') or index(description,'PROGRAF') then GNN='TACROLIMUS';
else if index(description,'TACROLIMUS') or index(description,'PROTOPIC') then GNN='TACROLIMUS';
else if index(description,'TACROLIMUS') or index(description,'TACROLIMUS') then GNN='TACROLIMUS';
else if index(description,'THIOMALATE') or index(description,'AUROLATE') then GNN='THIOMALATE';
else if index(description,'THIOMALATE') or index(description,'GOLD SODIUM') then GNN='THIOMALATE';
else if index(description,'THIOMALATE') or index(description,'MYOCHRYSINE') then GNN='THIOMALATE';
else if index(description,'TOCILIZUMAB') or index(description,'ACTEMRA') then GNN='TOCILIZUMAB';
else if index(description,'TOFACITINIB')  then GNN='TOFACITINIB';
else if index(description,'TOFACITINIB') or index(description,'XELJANZ') then GNN='TOFACITINIB';
else if index(description,'USTEKINUMAB') or index(description,'STELARA') then GNN='USTEKINUMAB';
else if index(description,'VEDOLIZUMAB') or index(description,'ENTYVIO') then GNN='VEDOLIZUMAB';
